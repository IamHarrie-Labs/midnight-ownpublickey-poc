# ownPublicKey() Access Control Vulnerability — Proof of Concept

Companion code for the tutorial:
**[The Zero-Knowledge Trap: Why ownPublicKey() Cannot Prove Identity in Compact](https://dev.to/iamharrie/the-zero-knowledge-trap-why-ownpublickey-cannot-prove-identity-in-compact-169i)**

Submitted as part of the [Midnight Eclipse Content Bounty Program](https://forum.midnight.network/t/midnight-content-bounty-program-eclipse/1148) — Issue [#295](https://github.com/midnightntwrk/contributor-hub/issues/295).

---

## Repository Structure

```
midnight-ownpublickey-poc/
├── contracts/
│   ├── vulnerable.compact        # Uses ownPublicKey() — bypassable by any attacker
│   ├── secure.compact            # Uses witness + persistentHash — cryptographically sound
│   └── managed/                  # Compiled artifacts (generated by CI or compact compile)
├── src/
│   ├── config.ts                 # Network endpoint configuration
│   ├── witnesses.ts              # Off-chain witness providers for both contracts
│   ├── deploy-vulnerable.ts      # Deploy vulnerable vault + owner deposit
│   ├── attack.ts                 # 4-step exploit against vulnerable vault
│   └── attack-secure.ts         # Same attack against secure vault — fails
├── test/
│   └── exploit.test.ts           # Automated demonstration (no network required)
├── .github/workflows/ci.yml      # Compile contracts + run tests on push
├── docker-compose.yml            # Local Midnight node + proof server + indexer
├── package.json
├── tsconfig.json
└── README.md
```

---

## The Vulnerability

`ownPublicKey()` in Compact compiles to an **unconstrained `private_input`** in the ZK circuit. Any value can be provided — including the stored owner key read from public ledger state — without knowing any secret key.

```compact
// vulnerable.compact
export circuit withdraw(): [] {
  assert(ownPublicKey() == vault_owner, "Not the vault owner");
}
```

`vault_owner` is public ledger state. An attacker can:

1. Read `vault_owner` from the chain
2. Send a prove request with `own_public_key = vault_owner` to the proof server
3. The circuit evaluates `assert(vault_owner == vault_owner)` → `TRUE`
4. Submit the transaction — no secret key required

---

## The Fix

```compact
// secure.compact
witness localSecretKey(): Bytes<32>;

circuit ownerCommitment(sk: Bytes<32>): Bytes<32> {
  return persistentHash<Vector<2, Bytes<32>>>([pad(32, "vault:owner:"), sk]);
}

export circuit withdraw(): [] {
  assert(ownerCommitment(localSecretKey()) == vault_owner, "Not the vault owner");
}
```

The ZK proof now proves: *"I know an `sk` such that `hash("vault:owner:", sk) == vault_owner`."*

An attacker who reads `vault_owner` cannot reverse `persistentHash` to find `sk`. No valid proof is possible without the original secret key.

This is the same pattern used in [midnightntwrk/example-bboard](https://github.com/midnightntwrk/example-bboard).

---

## Running the PoC

### Step 1 — Start the local stack

```bash
docker compose up -d
# Starts: proof server (6300), Midnight node (9933/9944), indexer (8088)
```

### Step 2 — Compile the contracts

```bash
compact update
compact compile contracts/vulnerable.compact contracts/managed/vulnerable
compact compile contracts/secure.compact contracts/managed/secure
```

### Step 3 — Install dependencies

```bash
npm install
```

### Step 4 — Run the automated tests (no network required)

```bash
npm test
```

Expected output:
```
  vulnerable.compact — ownPublicKey() access control
    ✓ legitimate owner can withdraw (normal usage)
    ✓ [CVE] attacker bypasses access control using vault_owner read from ledger
    ✓ [CVE] attacker does not need the owner's secret key or wallet

  secure.compact — persistentHash commitment
    ✓ legitimate owner can withdraw with their secret key
    ✓ [Fix] attacker cannot use stored vault_owner as the secret key
    ✓ [Fix] attacker cannot use a random key
    ✓ [Fix] attacker cannot use the vulnerable contract's stored public key as sk

  persistentHash — one-way commitment
    ✓ commitment is deterministic (same sk → same commitment)
    ✓ commitment changes when sk changes
    ✓ domain prefix prevents cross-context reuse
    ✓ brute-force 1-byte key space cannot invert a real 32-byte commitment

Tests: 11 passed, 11 total
```

### Step 5 — Run the end-to-end exploit (requires running stack)

```bash
# Deploy vulnerable vault and register as owner
npx ts-node src/deploy-vulnerable.ts

# Execute the 4-step exploit — expect EXPLOIT COMPLETE in output
npx ts-node src/attack.ts

# Attempt same attack against secure vault — expect ATTACK FAILED
npx ts-node src/attack-secure.ts
```

---

## Compilation Output

Contracts compiled and verified against **compactc 0.31.0 (language version 0.23.0)** via CI:

```
=== Compiler version ===
compact 0.31.0
=== Language version ===
language 0.23.0

✅ vulnerable.compact compiled
✅ secure.compact compiled
```

CI build: see [`.github/workflows/ci.yml`](.github/workflows/ci.yml)

---

## Affected Production Code

OpenZeppelin's `Ownable.compact` uses `ownPublicKey()` in `assertOnlyOwner()`:

```compact
export circuit assertOnlyOwner(): [] {
  Initializable_assertInitialized();
  const caller = ownPublicKey();
  assert(caller == _owner.left, "Ownable: caller is not the owner");
}
```

Every contract that gates privileged operations behind `assertOnlyOwner()` is currently bypassable using the attack above.

---

## Reference

- [example-bboard](https://github.com/midnightntwrk/example-bboard) — correct witness + persistentHash pattern
- [OpenZeppelin compact-contracts](https://github.com/OpenZeppelin/compact-contracts) — affected library
- [Midnight Docs](https://docs.midnight.network)
- [Bounty #295](https://github.com/midnightntwrk/contributor-hub/issues/295)
